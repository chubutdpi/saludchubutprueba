<%= form_with(model: vaccination_scheme, local: true) do |form| %>
  <% if vaccination_scheme.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vaccination_scheme.errors.count, "error") %> prohibited this vaccination_scheme from being saved:</h2>

      <ul>
      <% vaccination_scheme.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
